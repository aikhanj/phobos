import type { BiosignalState } from '@phobos/types';

// Phase 4: Local orchestrator — queries sub-directors, synthesizes SceneConfig
export class ScareDirector {
  async tick(_biosignals: BiosignalState): Promise<void> { /* stub */ }
}
